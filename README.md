# Financial App (Spring Boot CRUD)

This project provides CRUD operations for Users, Accounts, Payees, and Transactions.

## How to Run
1. Ensure PostgreSQL is running and update `application.properties` with correct DB details.
2. Build and run:
   ```bash
   mvn clean install
   mvn spring-boot:run
   ```
